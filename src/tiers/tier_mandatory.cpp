/* ─── Tier: mandatory ───
**
** The pure RFC 2812 kernel the subject grades. No extensions: no bot, no
** file transfer, no platform features. Built by `make mandatory`.
*/

#include "ext/RegisterExtensions.hpp"

void registerExtensions(Server &server)
{
	(void)server;
}
